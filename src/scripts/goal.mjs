export default class Goal{
    constructor(id, name)
    {
        this.id = id;
        this.name = name;
    }
    init() {
        //this.list = getLocalStorage('so-cart');
        //this.calculateItemSummary();
    }
}